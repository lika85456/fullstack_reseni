CREATE TABLE Country (countryId INT NOT NULL PRIMARY KEY ,name NVARCHAR(100) NOT NULL,code CHAR(2) NOT NULL,continentName NVARCHAR(100) NOT NULL,citizenCount INT NULL);CREATE TABLE City (cityId INT NOT NULL PRIMARY KEY ,name NVARCHAR(100) NOT NULL,countryId INT NOT NULL REFERENCES Country(countryId),citizenCount INT NOT NULL);